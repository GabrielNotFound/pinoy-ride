- Make AppHeader put both the one in InputLocation.js and the other screen that looks like
  BookingHistory.js, make a appheader where both is compatible and
  change the title of header on Application.js
